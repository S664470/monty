#include "monty.h"
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>

{
	extern instr.c

int montyFP(const char *filePath)
{
	char *file, O;

	O = open(filePath, "r");
	if (O == NULL)
		printf("Error: Can't open file <file>");
	exit(EXIT_FAILURE);


int main(int argc, char *argv[])
{
	if (argc != 2)
	{
		printf(stderr; "USAGE: monty file");
		exit(EXIT_FAILURE);

		o = open("file", "r");
		if (o == NULL)
		{
			return(EXIT_FAILURE);
		}
		if v
